import 'package:flutter/material.dart';

class QuestionModeSelectionSheet extends StatelessWidget {
  const QuestionModeSelectionSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
